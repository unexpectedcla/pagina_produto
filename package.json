{
  "name": "enext",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -Rf ./dist",
    "start": "npm run clean && gulp --colors && gulp watch --colors",
    "build": "npm run clean && gulp --colors --production",
    "vtex": "npm run build && gulp watch --colors --production"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bialamano/programa-de-capacitacao"
  },
  "license": "MIT",
  "homepage": "",
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-google": "^0.9.1",
    "gulp": "^3.9.1",
    "taskerify": "^1.9.4",
    "underscore": "^1.9.0"
  },
  "dependencies": {
    "intersection-observer": "^0.5.1",
    "nouislider": "^14.1.1",
    "npm": "^6.9.0",
    "vtex-catalog": "^1.3.0",
    "vtex-masterdata": "^0.4.1",
    "vtex-minicart": "^0.5.5",
    "vtex-recently-viewed": "0.0.1",
    "vtex-shelf-properties": "^0.4.0",
    "vtex-utils": "^1.18.1",
    "vtex-wishlist": "^0.6.0"
  },
  "engines": {
    "node": ">= 6.0.0"
  }
}
